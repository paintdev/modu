import "base64" as base64;

let string = input("String to encode: ");
print("Base64: ", base64.encode(string));

let encoded = input("\nBase64 to decode: ");
print("Decoded: ", base64.decode(encoded));